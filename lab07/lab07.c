#include <stdio.h>
#include <stdlib.h>

#include "arvore.h"

p_node fill_tree(Data v) {

}

int main() {
   int m;
   char* buffer;
   scanf("%d", &m);

   for (int i = 0; i < m; i++)
   {
      scanf(" %s", &buffer);
   }
   



   return EXIT_SUCCESS;
}
